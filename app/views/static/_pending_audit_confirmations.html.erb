<% pending_audit_confirmations.each do |pending_audit_confirmation| %>
  <%= link_to "No Overtime for the week of #{pending_audit_confirmation.start_date}", root_path, 
      class: "btn btn-primary btn-block btn-lg", data: { confirm: 'Confirm you did not have any overtime' }  %>
<% end %>